import express from "express";
const router = express.Router();

// const _SettingsController = require("../controllers/_SettingsController");

// router.get("/reset-db-seed", _SettingsController.reset);
// router.get("/reset-status", _SettingsController.status);
// router.get("/reset-log", _SettingsController.resetLog);

export default router;
